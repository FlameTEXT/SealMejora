const config = {
  // 扩展名
  "name": "two_sum",
  // 扩展作者
  "author": "nao",
  // 项目主页
  "homepageURL": "https://github.com/FlameTEXT/SealMejora",
}

export default {
  ...config,

  // 输出文件名
  outfile: config.name+".js"
}
